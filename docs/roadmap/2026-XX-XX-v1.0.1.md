# v1.0.1 Follow-up Bucket (placeholder — date when used)

**Status**: placeholder. Rename to `2026-MM-DD-v1.0.1.md` when v1.0.1 work begins.

This is the landing zone for things deliberately deferred from v0.9.0
TestFlight and v1.0 public release. Nothing here is a blocker for
either of those — but each is real, tracked, and should be picked up
during the v1.0.1 patch cycle.

## Deferred from earlier sessions

### MetricKit subscriber (Session 10 audit finding)

`CLAUDE.md` tech stack lists "Crash reporting: MetricKit (on-device,
opt-in upload only)" — but no `MXMetricManager` subscriber is wired in
production. TestFlight collects crashes via the ASC system regardless,
so v0.9.0 doesn't blow up without it. But for richer signals (hang
reports, energy logs, disk-write logs) post-launch, wire MetricKit.

Scope: ~50 LOC. Make it a real on-device-only persistence (don't ship
the upload path until Pillar-1-compliant opt-in UX is designed
separately).

### Localizable.strings line-count drift

DE 271 lines, EN 272. Keys are at parity per the awk-diff in Session 10.
The extra line is presumably a comment or blank-line in EN. Harmless
but worth eyeballing during v1.0.1 cleanup.

### Dynamic Type beyond the 5 surfaces

Session 8 #132 covered CyclePhaseStrip + MoreSettingsSheet at AX5.
~195 `.font(.system(size:))` sites remain across the rest of the
codebase. Walk them in v1.0.1 batches. The audit doc-comment in
`smoke-test-plan.md` step S12 documents which surfaces still risk
truncation.

## Deferred per memory / decision

### #126 NEW-G iCloud private DB sync (opt-in)

Memory note `[[project-tideline-roadmap]]`: "Post-launch." Design work
required before implementation (which entity types sync? conflict
resolution? what UX makes the opt-in clear without scaring users?).

### #127 NEW-H Adolescent (<16) onboarding flow

Memory: "Post-launch." Different μ + β prior for users <16 since
their cycles aren't yet stable.

### #154 Mein Zyklus Layer 2 v1.1 pattern extensions (B3+B4+C3)

Memory `[[project-tideline-roadmap]]`: "Phase 4 post-beta." Wait for
beta feedback first; let real-user pattern observations drive which
patterns are worth detecting.

## From TestFlight feedback (fills in during the beta period)

Each item gets:
- One-line description
- Reporter (initials only; not full names)
- Priority (P0–P3 per `post-launch-watch.md` criteria)
- Status (open / fixed in v1.0.1 / deferred)

```
- [ ] (placeholder) Example: "Layer 2 pattern badge disappears on PCOS
      switch" — reporter X — P2 — open
```

## NOT for v1.0.1

The Phase-3/4/5 items from the tracker (#138, #139, #140, #142, #144,
#145, #146, #147, #151, #152, #153) are larger features. They go in
v1.1, v1.2, etc. — not in a patch release.

## When v1.0.1 ships

Trigger criteria:
- Hotfix for any P0 (same-day)
- Hotfix for any P1 (within 1 week)
- Otherwise: batch up P2 items + small fixes; ship when ~5 items have
  accumulated

Build-number sequence: 0.9.0 build 2 (TestFlight) → 0.9.0 build 3
(hotfix during beta) → 0.9.1 (next named version after substantive
feedback) → 1.0 (public release) → 1.0.1 (first patch post-release).
